version: '3.8'
services:
  app:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    command: bash -lc "pip install -r requirements.txt && python backend/app.py"
